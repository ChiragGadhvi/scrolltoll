package com.chirag.scrolltoll

import io.flutter.embedding.android.FlutterActivity

/**
 * Plain Flutter host. The `com.chirag.scrolltoll/overlay` MethodChannel that
 * used to live here drove the Live Bubble overlay, which was removed along with
 * its foreground service and the SYSTEM_ALERT_WINDOW permission.
 */
class MainActivity : FlutterActivity()
